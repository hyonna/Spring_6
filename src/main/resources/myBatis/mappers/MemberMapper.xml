<?xml version="1.0" encoding="UTF-8"?>
<!DOCTYPE mapper
  PUBLIC "-//mybatis.org//DTD Mapper 3.0//EN"
  "http://mybatis.org/dtd/mybatis-3-mapper.dtd">
 <mapper namespace="MemberMapper">
 	
 	<sql id="colums">
 		id, pw, name, email, grade
 	</sql>
 	
 	<insert id="setWrite" parameterType="com.iu.member.MemberDTO">
 		insert into member (<include refid="colums" />) values(#{id}, #{pw}, #{name}, #{email}, 3)
 	</insert>
 	<select id="getSelect" parameterType="com.iu.member.MemberDTO" resultType="com.iu.member.MemberDTO">
 		select <include refid="colums" /> from member where id=#{id} and pw=#{pw}
 	</select>
 
 </mapper>