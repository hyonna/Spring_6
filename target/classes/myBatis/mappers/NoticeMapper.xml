<?xml version="1.0" encoding="UTF-8"?>
<!DOCTYPE mapper
  PUBLIC "-//mybatis.org//DTD Mapper 3.0//EN"
  "http://mybatis.org/dtd/mybatis-3-mapper.dtd">
 <mapper namespace="NoticeMapper">
 	<insert id="noticeWrite" parameterType="com.iu.board.BoardDTO">
 		insert into notice values(notice_seq.nextval, #{title}, #{writer}, #{contents}, sysdate, 0)
 	</insert>
 	<delete id="noticeDelete" parameterType="java.lang.Integer">
 		delete notice where num=#{num}
 	</delete>
 	<update id="noticeUpdate" parameterType="com.iu.board.notice.NoticeDTO">
 		update notice set title=#{title}, writer=#{writer}, contents=#{contents} where num=#{num}
 	</update>
 	<select id="noticeSelect" parameterType="java.lang.Integer" resultType="com.iu.board.notice.NoticeDTO">
 		select num, title, writer, contents, reg_date, hit from notice where num=#{num}
 	</select>
 	
 	<select id="noticeList" resultType="com.iu.board.notice.NoticeDTO">
 		select * from
 		(select rownum R, N.* from
 		(select * from notice order by num desc) N)
 		where R between 1 and 10
 	</select>
 </mapper>